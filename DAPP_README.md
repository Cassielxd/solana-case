# 🚀 Solana DApp 完整平台

一个完整的 Solana 去中心化应用平台，包含后端 API 服务和 Vue 3 前端界面，支持所有 4 个 Solana 程序。

## 📚 项目概述

### 架构

```
┌─────────────────────────────────────────────────────────┐
│                    Solana Blockchain                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ Counter  │ │  Vault   │ │ Profile  │ │   AMM    │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
└─────────────────────────────────────────────────────────┘
                           ↕
┌─────────────────────────────────────────────────────────┐
│              Backend API Server (Express)                │
│          提供 REST API 封装 Solana 程序调用              │
└─────────────────────────────────────────────────────────┘
                           ↕
┌─────────────────────────────────────────────────────────┐
│           Frontend DApp (Vue 3 + TypeScript)            │
│      用户界面 + Phantom 钱包集成 + 程序交互界面          │
└─────────────────────────────────────────────────────────┘
```

### 包含的程序

1. **Counter** 🔢 - 简单计数器程序
2. **Token Vault** 💰 - SOL 金库程序
3. **User Profile** 👤 - 用户资料管理程序
4. **Simple AMM** 💱 - 自动做市商程序

## 🎯 快速开始

### 前置要求

- ✅ Node.js (v18+)
- ✅ Yarn
- ✅ Solana CLI
- ✅ Anchor CLI
- ✅ Phantom 钱包浏览器扩展

### 安装依赖

```bash
# 安装所有依赖（包括后端和前端）
yarn install
```

### 启动完整系统

#### 方法 1：一键启动（推荐）

```bash
# 同时启动后端和前端
yarn dev
```

这将并行启动：
- 后端 API 服务器: `http://localhost:3001`
- 前端开发服务器: `http://localhost:5173`

#### 方法 2：分别启动

**终端 1 - 启动后端**
```bash
yarn server:dev
```

**终端 2 - 启动前端**
```bash
yarn frontend:dev
```

### 前置步骤

在使用 DApp 之前，确保：

1. **启动本地 Solana 验证器**
   ```bash
   solana-test-validator
   ```

2. **部署所有程序**
   ```bash
   anchor build
   anchor deploy
   ```

3. **配置 Phantom 钱包**
   - 安装 Phantom 浏览器扩展
   - 切换到本地网络（Localhost）
   - 确保有足够的 SOL（可通过 `solana airdrop 2` 获取）

## 📦 Workspace 结构

项目使用 Yarn Workspace 管理：

```
my-project/                    # 根目录
├── programs/                  # Solana 程序
│   ├── my-project/           # Counter 程序
│   ├── token-vault/          # Vault 程序
│   ├── user-profile/         # Profile 程序
│   └── simple-amm/           # AMM 程序
├── server/                    # 后端 API 服务 ⭐
│   ├── src/
│   │   ├── config/           # Solana 配置
│   │   ├── services/         # 业务逻辑
│   │   ├── routes/           # API 路由
│   │   └── index.ts          # 服务器入口
│   ├── package.json
│   └── README.md
├── dapp-frontend/             # Vue 3 前端 ⭐
│   ├── src/
│   │   ├── api/              # API 客户端
│   │   ├── components/       # Vue 组件
│   │   ├── views/            # 页面视图
│   │   ├── stores/           # 状态管理
│   │   ├── router/           # 路由配置
│   │   └── App.vue
│   ├── package.json
│   └── README.md
├── tests/                     # Anchor 测试
├── client-ts/                 # TypeScript 客户端示例
├── package.json               # Workspace 配置 ⭐
└── DAPP_README.md             # 本文件
```

## 🎮 使用指南

### 1. 打开 DApp

访问 `http://localhost:5173`

### 2. 连接钱包

点击右上角"连接钱包"按钮，授权 Phantom 钱包。

### 3. 使用各个程序

#### Counter（计数器）

1. 进入 Counter 页面
2. 点击"初始化计数器"（首次使用）
3. 使用"增加"和"减少"按钮

#### AMM（自动做市商）

1. 进入 AMM 页面
2. 输入 Token A 和 Token B 的 Mint 地址
3. 查询池信息或初始化新池
4. 获取交换报价

#### Vault 和 Profile

UI 界面已创建（占位页面），功能待完善。

## 🔧 开发

### 可用命令

```bash
# 开发
yarn dev                    # 同时启动后端和前端
yarn server:dev             # 仅启动后端
yarn frontend:dev           # 仅启动前端

# 构建
yarn build                  # 构建后端和前端
yarn server:build           # 仅构建后端
yarn frontend:build         # 仅构建前端

# 运行
yarn server:start           # 运行生产版后端

# 代码格式化
yarn lint                   # 检查代码格式
yarn lint:fix               # 修复代码格式
```

### 后端 API 端点

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/health` | GET | 健康检查 |
| `/api/programs` | GET | 获取程序列表 |
| `/api/counter/:publicKey` | GET | 获取计数器 |
| `/api/counter/initialize` | POST | 初始化计数器 |
| `/api/counter/increment` | POST | 增加计数 |
| `/api/counter/decrement` | POST | 减少计数 |
| `/api/amm/pool` | GET | 获取流动性池 |
| `/api/amm/pool/initialize` | POST | 初始化池 |
| `/api/amm/swap/quote` | POST | 获取交换报价 |

详细 API 文档：[server/README.md](server/README.md)

### 前端技术栈

- **Vue 3** + **Composition API**
- **TypeScript** - 类型安全
- **Vite** - 开发服务器
- **Element Plus** - UI 组件
- **Pinia** - 状态管理
- **Vue Router** - 路由
- **Axios** - HTTP 客户端

详细前端文档：[dapp-frontend/README.md](dapp-frontend/README.md)

## 🐛 常见问题

### 后端无法连接 Solana

- 确保 `solana-test-validator` 正在运行
- 检查 `server/.env` 中的 RPC URL 配置
- 验证钱包密钥文件路径正确

### 前端无法调用 API

- 确保后端服务运行在 3001 端口
- 检查浏览器控制台的网络错误
- 验证 Vite 代理配置（`dapp-frontend/vite.config.ts`）

### 钱包连接失败

- 确保已安装 Phantom 钱包扩展
- 检查钱包是否切换到正确的网络
- 尝试刷新页面或重启浏览器

### 交易失败

- 检查钱包余额是否足够
- 确认程序已正确部署
- 查看浏览器控制台和后端日志

## 📊 项目特点

### ✅ 已实现

- ✅ 完整的 Workspace 架构
- ✅ Express 后端 API 服务
- ✅ Vue 3 现代化前端
- ✅ Phantom 钱包集成
- ✅ Counter 程序完整功能
- ✅ AMM 池查询和报价
- ✅ 响应式 UI 设计
- ✅ TypeScript 全栈类型安全

### 🚧 待完善

- ⏳ AMM 完整交互（添加/移除流动性、交换）
- ⏳ Vault 程序 UI
- ⏳ Profile 程序 UI
- ⏳ 交易历史记录
- ⏳ 错误处理优化
- ⏳ 单元测试

## 🔐 安全注意事项

- 🔒 **永远不要**提交钱包私钥
- 🔒 使用环境变量管理敏感信息
- 🔒 测试环境使用测试代币
- 🔒 生产部署前进行安全审计

## 📝 贡献

欢迎贡献！请查看各子项目的 README：
- [后端 README](server/README.md)
- [前端 README](dapp-frontend/README.md)

## 📄 许可证

ISC

---

## 🙏 致谢

- [Solana](https://solana.com/) - 高性能区块链
- [Anchor](https://www.anchor-lang.com/) - Solana 开发框架
- [Vue.js](https://vuejs.org/) - 渐进式 JavaScript 框架
- [Element Plus](https://element-plus.org/) - Vue 3 UI 库

---

**⭐ 如果这个项目对你有帮助，请给它一个 Star！**

Made with ❤️ for the Solana community
